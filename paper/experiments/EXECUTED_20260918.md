# Executed information-matched control

## Commands and observations

```bash
python -m unittest discover -s src/S04_Tests -v
python experiments/p19/toy.py --output results/paired_control
bash scripts/build_paper.sh build/p20
```

The local scientific slice passed 24 tests, including finite-query union
emulation across multiple query/support budgets, redundant dictionaries and
zero responses. The full inherited template tests require the complete checkout
and are exercised by the pull-request workflow, not claimed as local tests.

The controlled entry produced 3,360 unit-method rows: four query budgets, seven
reported rules and 120 test units per budget. The maximum route/emulator
prediction discrepancy was zero at every budget. At Q=3, mean response MSE was
0.1818393713 for plain union-OMP, 0.0432767485 for both route and matched union,
and 0.2082589102 after descriptor shuffling. The full summary is retained in
`results/paired_control_summary.json`; raw unit CSV is regenerated by the command.
The follow-up controls were motivated by the previous exploratory result and
are not represented as preregistered independent confirmation.

The reading manuscript compiled to nine A4 pages. All 37 cited keys resolved,
with zero uncited bibliography entries. The SVG contains 28 text elements, six
rectangles, four lines and one arrowhead path, with no image element or base64.
PDF pages and the 3,000-pixel figure preview were visually inspected. This is not
a claim that the external nature-figure audit suite ran.

## Scientific consequence

The same-information union control invalidates an intrinsic routing-class
interpretation of the plain-union comparison. Independent real-data evidence for
the shared-coordinate learning objective remains absent. No PHMFactory pointer,
benchmark adapter, GPU experiment or SOTA result was promoted in this slice.
